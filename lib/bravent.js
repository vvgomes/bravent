import defineAggregate from "./aggregate";
import Validation from "data.validation";

const Success = Validation.Success;
const Failure = Validation.Failure;

export { defineAggregate, Success, Failure };
